<!-- Parent: ../AGENTS.md -->
<!-- Generated: 2026-03-25 | Updated: 2026-03-25 -->

# src/shared/ — Boiler-Template Synced Files

## Purpose

Deterministic utility code synced from obsidian-boiler-template. Every Obsidian plugin in the ecosystem uses identical copies. Changes here propagate via `sync:plugins` or `obsidian-propagate` skill.

## Key Files

| File | Purpose |
|------|---------|
| `plugin-logger.ts` | Unified logging interface (debug, info, warn, error) with console + Notice output |
| `plugin-notices.ts` | Catalog-driven notice system with timeout, mutability, and template expansion |
| `debounce-controller.ts` | Debounced state machine (dirty flag, running state, retry logic) |
| `settings-migration.ts` | Settings schema versioning and upgrade path helpers |

## For AI Agents

**All agents:** Read boiler-template source files to understand canonical patterns before modifying synced code.

**obsidian-developer:** Fixes bugs in shared code, then propagates to boiler-template and all downstream plugins.

**obsidian-ui:** Uses PluginLogger, PluginNotices for user-facing messages; never logs directly to console.

**obsidian-qa:** Verifies no divergence from boiler-template; all changes go through propagation workflow.

## Source of Truth

**Boiler-template location:** `/Users/beomsu/Documents/Dev/Obsidian-Plugins/obsidian-boiler-template/tooling/shared/`

**Sync workflow:**
1. Prove pattern in boiler-template first
2. Run `pnpm sync:plugins` to propagate
3. Never edit synced files directly in plugins

**Propagation tools:**
- `obsidian-propagate` skill (AI-assisted)
- `pnpm sync:plugins` command
- Boiler-template is the canonical source

## Key Patterns

**PluginLogger:**
- Shared prefix for all log lines [QMD]
- Debug-gated via callback (no console spam by default)
- noticeError() combines console + Notice

**PluginNotices:**
- Catalog maps notice keys to templates + timeouts
- Supports {{ variable }} substitution
- Immutable notices (disable user muting)

**DebounceController:**
- Debounce + dirty flag + retry queue
- Prevents concurrent runs while respecting new mutations
- No obsidian imports; pure state machine

**SettingsMigration:**
- Version field in settings object
- Per-version upgrade functions
- Graceful fallback to defaults

## Sync Status

All files include header comment:
```
// Synced from obsidian-boiler-template/tooling/shared/…. Do not edit this file directly.
```
